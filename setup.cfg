[metadata]
name = csvmodel
version = 0

[options]
install_requires = 
    docopt
    jsonschema
    pydantic[email]
package_dir=
    =src
packages=find:

[options.entry_points]
console_scripts =
    csvmodel = csvmodel.main:main

[options.packages.find]
where=src
